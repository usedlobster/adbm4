<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(boot|img|css|js) - [L]
    RewriteRule ^ /boot/boot.php [L]
</IfModule>
<IfModule !mod_rewrite.c>
    Error "mod_rewrite is required"
</IfModule>