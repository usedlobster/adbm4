<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{HTTP:Authorization} ^(.*)
    RewriteRule .* - [e=HTTP_AUTHORIZATION:%1]
    RewriteRule ^(apiboot) - [L]
    RewriteRule ^ /apiboot/api-boot.php [L]
</IfModule>
<IfModule !mod_rewrite.c>
    Error "mod_rewrite is required"
</IfModule>